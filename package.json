{
  "name": "azure_dev_ops_gantt",
  "version": "0.0.1",
  "scripts": {
    "build:pug": "pug --out ./dist/ ./src/index.pug",
    "build:scss": "sass --style compressed ./src/index.scss ./dist/index.css",
    "build:ts": "tsc",
    "build": "npm run build:ts && npm run build:scss && npm run build:pug"
  },
  "devDependencies": {
    "pug-cli": "^1.0.0-alpha6",
    "sass": "^1.87.0",
    "typescript": "^5.8.3"
  }
}